name: Spark repository maintenance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "One-shot Spark rewrite preparation completed; retained temporarily for cleanup audit."
