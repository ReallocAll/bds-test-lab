name: Spark repository maintenance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Inert one-shot helper; delete after repository maintenance is complete."
