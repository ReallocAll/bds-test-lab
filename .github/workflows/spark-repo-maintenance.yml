name: Spark repository maintenance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Maintenance helper is inert pending deletion after Spark cleanup."
